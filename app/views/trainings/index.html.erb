Below is the day and time this session is held:

  <% @training.each do |training| %>
    <li><%= link_to "#{training.name}", training_path(training.id) %></li>
    <% end %>

<div><%= link_to "Back to Instructors", instructors_path %></div>
